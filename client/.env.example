# Both dev AND production use the relative path "/api" — requests are proxied
# to the backend (Vite proxy in dev; Vercel rewrite in prod, see vercel.json).
# This makes all API calls same-origin, so there is NO CORS in the browser.
VITE_API_URL=/api
