# Local dev: relative path → routed through the Vite proxy (see vite.config.js).
# This avoids CORS entirely in development.
VITE_API_URL=/api
# Production (Render API URL) — use the full absolute URL:
# VITE_API_URL=https://surya-store-api.onrender.com/api
